import TodoItem from "./TodoItem";

const TodoList = () => {
  return (
    <section className="todolist-container">
      <TodoItem />
      <TodoItem />
    </section>
  );
};

export default TodoList;
